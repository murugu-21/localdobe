---
import Base from '../layouts/Base.astro';
const description = 'localdobe processes every PDF locally in your browser — files never reach a server. This policy details exactly what data exists: hosting logs, site analytics, and on-device storage.';
---
<Base title="Privacy Policy — localdobe" {description} canonicalPath="/privacy">
  <article class="prose prose-slate mx-auto max-w-3xl px-4 pt-12 dark:prose-invert">
    <h1>Privacy Policy</h1>
    <p><em>Last updated: September 9, 2026</em></p>
    <p>
      The short version: <strong>your files never leave your device.</strong> Every tool on
      localdobe — merge, split, compress, edit, watermark, protect, unlock, and signature
      validation — runs entirely in your browser. When you
      drop a PDF into a tool, it's read into memory on your machine, processed there, and
      handed back to you as a download. At no point does your file — or any data derived
      from its contents — travel over the network to us or anyone else.
    </p>
    <p>
      The rest of this page describes, completely, the data that <em>does</em> exist when
      you use this site: standard hosting logs, site analytics, and a few preferences
      stored on your own device.
    </p>

    <h2>1. Your files</h2>
    <p>
      Files you process with localdobe's tools are opened in your browser's memory,
      transformed on your device, and offered back as a download. They are never uploaded,
      never logged, never cached on a server, and never seen by us. Passwords you enter in
      the Protect and Unlock tools are used only inside your browser's memory for the
      encryption or decryption operation and are likewise never transmitted or stored.
      After the page has loaded a tool once, most operations work with your network
      disconnected — the clearest proof that processing is local.
    </p>

    <h2>2. Hosting and server logs (Cloudflare)</h2>
    <p>
      The static site — HTML, CSS, JavaScript, fonts, and the files the tools
      use — is served by <a href="https://www.cloudflare.com/">Cloudflare</a>. Like any
      content delivery network, Cloudflare's edge may process and briefly log standard HTTP
      request metadata: the URL requested, your IP address, user-agent string, referrer,
      and timestamp. This covers <em>which pages and assets were requested</em> — never the
      contents of any file you process, because your files are not part of that traffic.
      Cloudflare processes this data as described in the
      <a href="https://www.cloudflare.com/privacypolicy/">Cloudflare Privacy Policy</a>.
    </p>

    <h2>3. Analytics (Cloudflare Web Analytics — cookieless)</h2>
    <p>
      We use <a href="https://www.cloudflare.com/web-analytics/">Cloudflare Web Analytics</a>
      to understand aggregate site usage — which pages are visited and how fast they load.
      We chose it because it is one of the least invasive analytics products available:
    </p>
    <ul>
      <li>it sets <strong>no cookies</strong> and uses no localStorage;</li>
      <li>it does <strong>not fingerprint</strong> your device or track you across sites;</li>
      <li>it collects page-view events only: the page URL, referrer, browser and device
        type, country-level location, and performance timings;</li>
      <li>it never sees your files or anything you do inside a tool — it has no access to
        file names, file contents, or tool inputs.</li>
    </ul>
    <p>
      This works via a small script served from <code>cloudflareinsights.com</code>. If you
      block it with an ad-blocker or a browser like Brave, every tool on this site continues
      to work exactly the same — we think that's how it should be. Analytics data is
      processed by Cloudflare under the
      <a href="https://www.cloudflare.com/privacypolicy/">Cloudflare Privacy Policy</a>.
    </p>

    <h2>4. Product analytics (PostHog)</h2>
    <p>
      We use <a href="https://posthog.com/">PostHog</a> to see which tools people use and
      where they get stuck — for example, how many visitors who open Compress PDF actually
      download a result, and which errors they hit on the way. Each event records the tool
      being used, the step reached, and coarse measurements of the work: the number of files
      or pages, file sizes in bytes, how long the operation took, and which options
      (compression preset, page size, split mode) were selected.
    </p>
    <ul>
      <li><strong>Nothing about your documents is included.</strong> Events never carry file
        names, file contents, page text, or passwords. Error messages are scrubbed of
        anything resembling a file name before they are sent.</li>
      <li><strong>No profile is built.</strong> PostHog is configured in cookieless mode
        with person profiles disabled: it writes no cookie and no
        <code>localStorage</code> entry, and there is no identifier that links your events
        together across visits or that could identify you.</li>
      <li><strong>Blocking it changes nothing.</strong> If an ad-blocker stops the PostHog
        script, every tool keeps working exactly the same.</li>
      <li><strong>It is served from our own subdomain.</strong> To keep these measurements
        from being skewed by ad-blockers, the PostHog script and its events go through
        <code>e.localdobe.com</code>, which forwards to PostHog. We mention it because it
        means the traffic is not obviously PostHog's if you inspect it yourself — the data
        collected is exactly what is listed above, and no more.</li>
    </ul>
    <p>
      This data is processed by PostHog under the
      <a href="https://posthog.com/privacy">PostHog Privacy Policy</a>.
    </p>

    <h2>5. Session replays (Microsoft Clarity)</h2>
    <p>
      We use <a href="https://clarity.microsoft.com/">Microsoft Clarity</a> to see how the
      interface is actually used — where people click, how far they scroll, and where the UI
      confuses them — through aggregate heatmaps and anonymized session replays. Replays are
      what make it possible to work out <em>why</em> a tool went wrong for someone rather than
      just counting that it did. Clarity records interaction data (clicks, scrolls, page URLs,
      browser and device type), which Microsoft processes under the
      <a href="https://privacy.microsoft.com/privacystatement">Microsoft Privacy Statement</a>.
    </p>
    <ul>
      <li><strong>Your documents are excluded.</strong> Every tool's work area is marked with
        Clarity's masking attribute, so replays show placeholder boxes — never file names,
        document text, rendered pages, or anything you type into a tool (passwords included).</li>
      <li><strong>It records no product events.</strong> Clarity is used for replays only. The
        usage events in section 4 go to PostHog and are not sent to Microsoft.</li>
      <li><strong>No cookies.</strong> We never send Clarity a consent signal, so it runs in
        its cookieless mode and cannot recognize a returning browser. We have verified in a
        real browser that loading this site sets no cookies and writes nothing to local or
        session storage.</li>
      <li><strong>Blocking it changes nothing.</strong> As with all analytics here, if an
        ad-blocker stops the Clarity script (<code>clarity.ms</code>), every tool keeps
        working exactly the same.</li>
    </ul>

    <h2>6. Data stored on your device</h2>
    <p>
      localdobe stores a small amount of data <em>in your browser, on your device</em>, none
      of which is transmitted to anyone:
    </p>
    <ul>
      <li><strong>Theme preference</strong> — if you toggle dark mode, your choice is kept in
        <code>localStorage</code> so the site remembers it.</li>
      <li><strong>Offline caches</strong> — a service worker caches the site's pages and
        engines so the tools work offline and load faster. This is standard browser cache
        storage.</li>
    </ul>
    <p>
      You can remove all of it at any time through your browser's site-data settings
      (usually under "Cookies and site data" → localdobe.com → clear), which fully resets
      the site.
    </p>

    <h2>7. What we don't collect</h2>
    <p>
      We have no accounts, no sign-ups, and no way to associate site usage with a real-world
      identity. We run no advertising and never sell or share data. <strong>This site sets no
      cookies</strong> — it writes none of its own, PostHog is configured in cookieless mode,
      and Clarity is loaded without a consent signal so it runs cookieless too. We verified
      this in a real browser: loading the site sets no cookies, first- or third-party.
    </p>

    <h2>8. Your rights</h2>
    <p>
      Because localdobe itself stores no personal data about you, there is nothing for us to
      access, correct, export, or delete on request — the data described in sections 2–5 is
      processed by Cloudflare, PostHog, and Microsoft, and requests concerning it can be
      directed to them under their privacy policies. Data stored on your device (section 6)
      is under your direct control through your browser and can be cleared at any time.
    </p>

    <h2>9. Children</h2>
    <p>
      This site provides document utilities and does not target children. We do not
      knowingly collect personal information from anyone, children included — the site has
      no mechanism to do so.
    </p>

    <h2>10. Changes to this policy</h2>
    <p>
      If how the site works ever changes in a way that affects this page — a new analytics
      tool, a new third party, anything that touches data — we will update this policy and
      the "last updated" date above. The core promise will not change: your files are
      processed on your device, full stop.
    </p>

    <h2>11. Contact</h2>
    <p>
      Questions about this policy, or about anything on this site, can go to{" "}
      <a href="mailto:murugu2001@gmail.com" class="font-medium text-accent underline underline-offset-2 hover:text-ink">
        murugu2001@gmail.com
      </a>
      . In the same spirit as the rest of this page: that is a personal Gmail inbox, so an
      email you send puts your address and whatever you write in it into Google's hands
      under{" "}
      <a href="https://policies.google.com/privacy">Google's privacy policy</a> — writing to
      us is the one part of using localdobe that isn't local. We don't add the address to any
      mailing list, and there is no mailing list to add it to. Please don't attach the
      document a tool struggled with; we can't debug your file and would rather not receive
      it. A description of what it contains and which step failed is more useful anyway.
    </p>
  </article>
</Base>
